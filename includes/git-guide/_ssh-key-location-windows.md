??? info "Where are these keys stored? What's a public key?"
    By default, your SSH keys are stored in a hidden folder called `.ssh`
    inside your user directory.

    In **Windows Explorer**, you can find them at: `C:\Users\YourName\.ssh`

    ![SSH Keys in Windows Explorer](../../images/git-guide/ssh-keys-in-windows.png)
